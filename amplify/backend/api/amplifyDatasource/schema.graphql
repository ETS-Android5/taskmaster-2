type Task @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String!
  description: String
  dateCreated: AWSDateTime
  State: State
}

enum State {
    New
    Assigned
    In_progress
    Complete
}